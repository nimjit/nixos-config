# nixos-config

NixOS flake configuration for user `thijmen`. Manages packages, theming, dotfiles,
and services declaratively across three machines (desktop, laptop, USB install).

See [`CLAUDE.md`](CLAUDE.md) for full documentation: machine setup, theme system,
applications, shell aliases, neovim keybindings, plans directory workflow,
and maintenance instructions.
